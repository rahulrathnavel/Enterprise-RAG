services:
  enterprise-rag:
    build: .
    container_name: enterprise-rag-demo
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
